apiVersion: apiserver.config.k8s.io/v1
kind: EncryptionConfiguration
resources:
- resources:
  - secrets
  providers:
  - {{ features.encryption.algorithm }}:
      keys:
      - name: key1
        secret: {{ generated_key.stdout }}
  - identity: {}
